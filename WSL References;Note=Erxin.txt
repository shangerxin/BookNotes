WSL References;Note=Erxin


# Install ubuntu
- install wsl from windows store
- install distribution 
$ wsl --list --online 
$ wsl --install -d <name>

- for ubuntu, after install 

$ sudo apt update
$ sudo apt full-upgrade

    + optional install gui 
    
$ sudo apt install x11-apps

launch gui 
$ xeyes &

Leave xeyes opened and run the calculator xcalc with:

xcalc

https://ubuntu.com/tutorials/install-ubuntu-on-wsl2-on-windows-11-with-gui-support#1-overview


# Set wsl proxy 
sudo touch /etc/apt/apt.conf.d/proxy.conf
sudo vi /etc/apt/apt.conf.d/proxy.conf
Acquire::http::Proxy "http://127.0.0.1:1053/";/


# Change default wsl distribution 
wsl --setdefault ub2204-jenkins